{
  "name": "tgfx-web-test",
  "version": "1.0.0",
  "description": "TGFX Web Test Runner",
  "scripts": {
    "build": "emcmake cmake -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build -- -j 8 && npm run copy",
    "build:debug": "emcmake cmake -B build -DCMAKE_BUILD_TYPE=Debug && cmake --build build -- -j 8 && npm run copy",
    "copy": "cp build/tgfx-test.js build/tgfx-test.wasm build/tgfx-test.data . 2>/dev/null || true",
    "serve": "npx http-server -p 8082 -c-1",
    "test": "npm run build && npm run serve",
    "autotest": "node run_test.js",
    "clean": "rm -rf build tgfx-test.js tgfx-test.wasm tgfx-test.data"
  },
  "devDependencies": {
    "adm-zip": "^0.5.16",
    "http-server": "^14.1.1",
    "puppeteer": "^24.38.0"
  },
  "keywords": [
    "tgfx",
    "webgl",
    "testing"
  ],
  "author": "Tencent",
  "license": "BSD-3-Clause"
}
