#!/usr/bin/env node
process.chdir(__dirname);

const childProcess = require("child_process");
childProcess.execSync("depsync", {stdio: "inherit"});

const xcodeGen = require("path").resolve(__dirname, "../third_party/vendor_tools/xcode-gen");

childProcess.execSync(
    `node "${xcodeGen}" ../ -w -DTGFX_BUILD_HELLO2D=ON -DTGFX_USE_METAL=ON -p mac -o metal`,
    {stdio: "inherit"}
);

childProcess.execSync(
    `node "${xcodeGen}" ../ -w -DTGFX_BUILD_HELLO2D=ON -p mac -o opengl`,
    {stdio: "inherit"}
);
