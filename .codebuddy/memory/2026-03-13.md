# 2026-03-13

## TGFX 官网首页 HTML → PAGX 完整转换

将 tgfx.org 官网首页**完整页面**（1440×5917）转换为自包含 PAGX 文件，所有 17 张图片资源以 base64 内嵌。

### 产出文件
- `tgfx_homepage.pagx` — 完整页面 PAGX 文件（1.5MB，自包含，无外部依赖）
- `tgfx_homepage.png` — 渲染输出（1440×5917）

### 页面区域
1. **导航栏** (0-80px): Logo + 首页/下载/文档/案例 + CN/GitHub/论坛交流
2. **Hero区域** (80-660px): TGFX 标题 + 描述 + 按钮 + 右侧大 Logo + 背景图
3. **FEATURE 标签** (658-731px)
4. **Feature 1-6** (738-4182px): 交替左右布局，含 SVG 数字路径 + 标题 + 描述 + 插图
5. **USERS 区域** (4302-4920px): 标签 + 标题 + 6 个用户 Logo + More Users 按钮
6. **Download/CTA** (4920-5769px): 深蓝背景 + Installer 图 + 免费下载使用 + 即将发布按钮
7. **Footer** (5769-5917px): 分隔线 + 公司地址/版权/电话

### 技术细节
- 17 张图片（PNG/WEBP）全部以 `data:image/...;base64,...` 格式内嵌到 `<Image>` 资源中
- SVG 数字（1-6）提取 Path data 直接用 PAGX `<Path>` 矢量渲染，通过 Group scale 缩放
- 文字使用 PingFang SC 字体
- 页面主色调: #0066FF（蓝色）、#00173A（深蓝背景）、#333333（正文）
- 生成脚本: `/tmp/gen_pagx.py`（Python 脚本读取 base64 JSON 生成完整 PAGX）
